//Tienes un arreglo (llamado myArray) con 5 elementos (enteros en el rango de 1 a 100). 
//Escribe un programa en Java que imprima el número más alto del arreglo (Si se repite, solo imprimir una vez).
//El programa solo debe imprimir el número, sin ningún texto.

package package1;

public class test2 {

	static int[] myArray = {13, 2, 4, 35, 1};
	 
	 
	public static void main(String[] args) {
	    int max = Integer.MIN_VALUE;
	 
	    for (int i = 0; i < myArray.length; i++) {
	        if (myArray[i] > max) {
	            max = myArray[i];
	        }
	    }
	 
	    System.out.println(max);
	}
	
	
}
